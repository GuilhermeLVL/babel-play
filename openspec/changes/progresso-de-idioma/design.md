## Context

Nada de coleta nova: só agregação do que o schema já tem (postura de privacidade intacta).
Confiança acompanha cada número (padrão `Honestidade.tsx`): ranking com menos de N revisões
declara base fraca em vez de fingir precisão.
