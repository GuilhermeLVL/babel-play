A distincao que decide a acao: codigo que sobrou de algo removido x capacidade que nunca ganhou tela. Apagar a segunda destroi trabalho pronto e transforma lacuna de produto em lacuna de servidor. As seis viram achado do relatorio, com a mais seria — `DELETE /api/ai/credentials/:id` — indo para a Fase 4: quem cola uma chave de IA hoje nao consegue apaga-la pela interface.

O script se exclui da varredura: ele cita todas as rotas na lista de justificativas e, lendo a si mesmo, daria toda rota como viva — um portao que passa sempre e pior que portao nenhum.
