/* A curva do passe mudou para `src/core/passe.ts`: o servidor precisa dela para creditar os
   Créditos de cada casa da trilha paga. Reexportada daqui para nenhuma tela mudar de import. */
export * from '@core'
