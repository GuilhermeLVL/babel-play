Sempre contra COPIA do banco (`sqlite3 .backup`), `127.0.0.1`, servidor `dist-server/server.cjs` em `NODE_ENV=production` e `AUTH_REQUIRED=0`. A carga escreve de verdade; por isso cada medicao parte de uma copia intocada (`babel-pristine.db`). No modo cluster o script mata o PID que ele conhece, que e o primario: o supervisor sobe o conjunto; matar so um worker e cenario da Fase 5.
