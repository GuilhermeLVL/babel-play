So a segunda pergunta estava duplicada, em quatro variantes que diferiam apenas no tratamento do
espaco em branco. `wer.ts`, `chaveDedupLegada`, `import/anki.ts` e `exercicios/diff.ts` ficam de
fora, com motivo.
