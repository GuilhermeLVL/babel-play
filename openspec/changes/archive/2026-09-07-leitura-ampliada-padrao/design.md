## Context

"Leitura ampliada" NÃO é uma flag própria: é o perfil etário `senior` (`Personalizar.tsx:412`,
CSS `.age-senior` em `index.css:1269-1298`). Mudar o default afeta só quem nunca gravou
`babel.age_profile`; onboarding e Personalizar seguem sendo os lugares de troca.
