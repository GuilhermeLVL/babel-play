## What and why

<!-- One paragraph. Link the issue if there is one. -->

## How it was verified

- [ ] `npm run typecheck && npm run typecheck:core && npm run lint && npm test && npm run build`
- [ ] A test that fails before and passes after (or why not)
- [ ] No new `fetch('/api/…')` outside `src/data/api.ts`

## What does not work yet

<!-- Be honest; it saves everyone time. -->
